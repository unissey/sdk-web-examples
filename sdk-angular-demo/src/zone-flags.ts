// Disable some Zone.js patches
(window as any).__Zone_disable_toString = true; // disable patch toString
(window as any).__Zone_disable_getUserMedia = true; // disable patch getUserMedia
// (window as any).__Zone_disable_EventTarget = true; // Désactive le patching global des EventTarget
// (window as any).__zone_symbol__UNPATCHED_EVENTS = ['*']; // Désactive le patching de TOUS les événements
